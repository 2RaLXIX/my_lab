gitea:
  admin:
    username: gitea
    password: Spoon19
    email: admin@home.arpa

  config:
    database:
      DB_TYPE: sqlite3

persistence:
  enabled: true
  size: 3Gi

postgresql:
  enabled: false

postgresql-ha:
  enabled: false

valkey:
  enabled: false

valkey-cluster:
  enabled: false

service:
  http:
    type: ClusterIP

ingress:
  enabled: false